# Milestone 543 — `frus-fetch-example` denies a missing doc comment

Issue #14, continuing the sweep. Same shape as `frus-hello`: `FetchDemo` and `Msg`
are private, and the only public item the crate exposes is `run()`, generated by
`frus::main!(FetchDemo::default())` for `src/bin/frus-fetch-example.rs` to call.

`#![warn(missing_docs)]` found nothing — the macro-generated `run()` is not flagged,
and every local item (the state, both `Msg` variants, the `Application` methods,
`JOKE_URL`) was already documented. Straight to `#![deny(missing_docs)]`.
