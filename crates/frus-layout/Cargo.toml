[package]
name = "frus-layout"
description = "frus — layout engine (flexbox) on top of taffy"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
frus-core = { path = "../frus-core" }
taffy.workspace = true
