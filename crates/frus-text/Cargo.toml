[package]
name = "frus-text"
description = "frus — text shaping and measurement (cosmic-text)"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
frus-core = { path = "../frus-core" }
cosmic-text.workspace = true
