[package]
name = "frus-image"
description = "frus — image decoding (PNG/JPEG) into ImageData"
edition.workspace = true
rust-version.workspace = true
version.workspace = true
license.workspace = true
readme = "README.md"

[dependencies]
frus-core = { path = "../frus-core" }
image.workspace = true
