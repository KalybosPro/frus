[package]
name = "frus-core"
description = "frus — shared foundation types (geometry, colour)"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
bytemuck.workspace = true
